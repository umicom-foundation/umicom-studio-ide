# Umicom Workspace 0.4.0

This workspace contains Umicom Framework, the `umicom` generator and an empty `applications` directory.

Create an application from the workspace root:

```powershell
umicom framework create "My Application" console gtk web
```

Build it with the commands in the main beginner manual.
