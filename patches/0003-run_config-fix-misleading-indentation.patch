From 9d44be1 Mon Sep 17 00:00:00 2001
From: build-fix-bot <bot@umicom>
Date: Thu, 10 Oct 2024 12:00:00 +0000
Subject: [PATCH 3/4] run_config.c: fix misleading indentation

GCC warns that the single-line `if` does not guard subsequent frees.
Use braces and one line per statement for clarity.

--- a/src/run_config.c
+++ b/src/run_config.c
@@ -53,7 +53,11 @@ void umi_run_config_free(UmiRunConfig *r)
-  if(!r) return; g_free(r->cwd); g_free(r->argv_line); g_free(r->env_multiline); g_free(r);
+  if (!r) {
+    return;
+  }
+  g_free(r->cwd);
+  g_free(r->argv_line);
+  g_free(r->env_multiline);
+  g_free(r);
 }
